return {
    settings = {
        Lua = {
            completion = {
                callSnippet = "Both",
                keywordSnippet = "Both",
                displayContext = 2,
                postfix = ':', -- I perfer to use : instead of @ to trigger completion event 
            },
        }
    }
}
